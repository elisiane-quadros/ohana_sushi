import { Flex } from 'antd';
import { ReactNode } from 'react';
import { AddressCardContainer } from './styles';

// interface AddressCartProps {
//   // children: ReactNode
// }

const AddressCart = () => {
  return (
    <AddressCardContainer>
      <span>address</span>
    </AddressCardContainer>
  );
};

export default AddressCart;
